import React from 'react';
import {View,StyleSheet,Text,Button}from 'react-native';

const MapSafe=({navigation})=>{
    return<Text>
        MapSafe
        </Text>
};
const styles = StyleSheet.create({});

export default MapSafe;